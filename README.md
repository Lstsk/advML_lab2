U-Net Paper Reproduction

This project is a PyTorch reproduction of the U-Net paper for the Phase 2 course project.

Project layout:
- `model.py`: U-Net model implementation
- `dataset.py`: dataset loading for images and masks
- `train.py`: training loop
- `eval.py`: evaluation script
- `data/`: dataset files
- `results/`: checkpoints and outputs